@model IEnumerable<ScamAlert.Models.ScamReports>

@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>View</h2>

<div>
    <h4>Scam Reports</h4>
    <hr />
    <table class="table">
        <tr>
            <th>
                Username
            </th>
            <th>
                Report
            </th>
            <th>
                Time Posted
            </th>
        </tr>
        @foreach (var item in Model.First().ScamReportss)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.User.userName)</td>
                <td>@Html.DisplayFor(modelItem => item.report)</td>
                <td>@Html.DisplayFor(modelItem => item.timePosted)</td>
            </tr>
        }
    </table>
</div>
<p>
   @* @Html.ActionLink("Add your own report", "Index")*@
    @Html.ActionLink("Back to Scam List", "Index")
</p>
